# PricingSnapshot DTO relocation and record mapper

Task 1 relocates the seven-field `PricingSnapshot` data carrier from `domain` to `dto` and changes `PricingSnapshotMapper` to a record whose `ObjectMapper` component is rejected when null. The mapper continues to serialize and deserialize the same DTO shape and preserves its validation and exception messages. The necessary dependent domain-package test import was updated so the required deletion does not leave stale compilation references. **Watch for:** no concerns identified; the repository is untracked, so this review is based on the task package and current source rather than a Git range diff.

**Verdict**: APPROVED

## High-level view

The DTO is now an annotation-free Java record in `id.xyz.parkease.dto`, with the required component order and types. The old domain source is absent, and the complete reference search found no remaining production or test import of `id.xyz.parkease.domain.PricingSnapshot`.

`PricingSnapshotMapper` remains the single JSON boundary. Its record component is guarded in the compact constructor, while the existing null-input, blank-input, serialization, and deserialization behavior remains in the same methods with the specified exception messages.

## SPEC COMPLIANCE

All binding constraints are met. `src/main/java/id/xyz/parkease/dto/PricingSnapshot.java:1-13` declares the required DTO record and contains no JPA annotation or persistence import. Its seven components at lines 6-12 match the specified names, order, and types. The required old source is absent at `src/main/java/id/xyz/parkease/domain/PricingSnapshot.java`.

`src/main/java/id/xyz/parkease/mapper/PricingSnapshotMapper.java:9-15` declares `PricingSnapshotMapper(ObjectMapper objectMapper)` as a record and uses a compact constructor assigning `Objects.requireNonNull` with the required message. Lines 17-34 retain the existing JSON contracts: null snapshots are rejected, blank JSON produces the retained `IllegalArgumentException`, and Jackson processing failures retain the serialization and deserialization wrapper messages. All imports in the changed DTO, mapper, and tests are explicit.

The only direct consumers found by a repository-wide `PricingSnapshot` reference search are the mapper and the two stated tests. Both tests import `id.xyz.parkease.dto.PricingSnapshot` (`PricingSnapshotMapperTest.java:5` and `PricingSnapshotTest.java:3`), so the package move has no stale source-level callers. The dependent test import is a necessary compilation repair after deleting the old domain type, not unrelated scope expansion.

## TASK QUALITY

The migration is minimal: one DTO move, one mapper declaration/constructor adjustment, and the required test-import updates. No duplicate compatibility type or adapter was added, avoiding ambiguity over DTO ownership. The mapper remains in the mapper layer and does not leak serialization concerns into the DTO.

The reported targeted Gradle mapper test passed after the change; it exercises JSON round-trip behavior and the existing null, blank JSON, invalid JSON, and serialization-failure paths. I did not re-run that command, per the task instruction. Static review confirms the reported contracts against the current mapper source. No additional validation was run because the requested task-scoped test was explicitly not to be re-run.

<details>
<summary>Issues (0)</summary>

No actionable concerns.

</details>

<details>
<summary>File map</summary>

- `src/main/java/id/xyz/parkease/dto/PricingSnapshot.java` — new immutable DTO record.
- `src/main/java/id/xyz/parkease/domain/PricingSnapshot.java` — removed former domain record.
- `src/main/java/id/xyz/parkease/mapper/PricingSnapshotMapper.java` — mapper converted to a record and retargeted to the DTO.
- `src/test/java/id/xyz/parkease/mapper/PricingSnapshotMapperTest.java` — mapper test imports the DTO.
- `src/test/java/id/xyz/parkease/domain/PricingSnapshotTest.java` — dependent test imports the DTO after the domain source deletion.

A Git full-diff pointer is unavailable because the implementation is untracked; review scope is represented by `task-1-review-package.md` and the current sources above.

</details>
