# Reservation planned-window invariant

Task 3 adds a derived Bean Validation property that rejects equal or reversed planned timestamps while leaving existing endpoint `@NotNull` constraints responsible for absent values. The change is limited to `Reservation` and its equality-boundary test; no lifecycle, persistence, service, repository, API, or unrelated validation change was identified. Watch for: no blocking concerns found; reported targeted-test evidence was accepted and was not rerun as instructed.

**Verdict**: APPROVED

## High-level view

The invariant is enforced at the aggregate boundary through `@AssertTrue`, so Bean Validation rejects equal and reversed endpoints without moving the rule into service or repository layers. Its null guard preserves the established `@NotNull` contract and avoids duplicate or null-unsafe cross-field failures.

The test uses a fixed `OffsetDateTime` for the required equality boundary. It does not separately exercise a reversed pair, but the reviewed strict-order predicate (`plannedStart.isBefore(plannedEnd)`) statically rejects both; the task brief specifically required the equality test.

<details>
<summary>Issues (0)</summary>

No actionable findings. No CRITICAL, MAJOR, MEDIUM, MINOR, or scope findings were identified.

</details>

<details>
<summary>Details</summary>

## Strict ordering at the Bean Validation boundary

**SPEC COMPLIANCE — confirmed.** `Reservation.java:53-59` retains the existing `@NotNull` constraints on `plannedStart` and `plannedEnd`. `Reservation.java:100-102` adds `@AssertTrue(message = "Planned end must be after planned start")` and returns valid only when either endpoint is absent or `plannedStart.isBefore(plannedEnd)`. With both values non-null, equal and reversed endpoints both produce a Bean Validation violation. Null inputs cannot dereference either field and remain invalid under the existing required-field constraints.

## Scope and persistence boundary

**TASK QUALITY — confirmed.** The accessor at `Reservation.java:100-102` is a derived JavaBeans validation property. It does not alter JPA field mappings, `@PrePersist`, lifecycle transitions (`checkIn`, `markNoShow`, `complete`, `cancel`), repository queries, migrations, or APIs. The planned-window search found no second validator or duplicated rule in production code.

## Regression check for the equality boundary

**TASK QUALITY — confirmed.** `DomainValidationTest.java:42-50` builds a reservation with a complete required slot and identical fixed timestamps, then verifies `validator.validate(reservation)` contains a violation. Reported RED/GREEN results for the targeted suite were reviewed as supplied evidence; no tests were rerun, per instruction.

</details>

<details>
<summary>File map</summary>

- `src/main/java/id/xyz/parkease/domain/Reservation.java` — strict planned-window Bean Validation property.
- `src/test/java/id/xyz/parkease/domain/DomainValidationTest.java` — fixed-time equal-window validation regression test.

Full change context was supplied in `.superpowers/sdd/2026-09-06-parkease-sprint-1-remediation/task-3-review-package.md`; the repository baseline contains only the initial tracked skeleton, while Task 3 source is untracked.

</details>
