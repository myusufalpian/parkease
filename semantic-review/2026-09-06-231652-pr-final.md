# Sprint 1 foundation remediation

This final review evaluates the current, uncommitted Sprint 1 remediation sources named in the final review package against the approved design and implementation plan. `PricingSnapshot` is now a seven-component DTO record and its mapper retains the JSON boundary while enforcing a non-null `ObjectMapper`. The reservation exclusion constraint now matches timezone-aware columns, and the entity rejects non-null equal or reversed planning windows without expanding into Sprint 2 lifecycle work. **Watch for:** no unresolved Critical or Important issue was identified; the supplied full-suite result was accepted and not rerun as requested.

**Verdict**: APPROVED

## High-level view

The DTO move is complete at the source level: the only current `PricingSnapshot` consumers are the mapper and its two tests, all importing `id.xyz.parkease.dto.PricingSnapshot`. The mapper’s record component is guarded in its compact constructor, and JSON validation and failures remain centralized rather than leaking into the domain.

The database rule uses `tstzrange` over the zoned reservation columns, scopes conflicts by `slot_id`, preserves half-open intervals, and only blocks `PENDING` and `ACTIVE` rows. V1 describes that V2-owned constraint without inventing a schema object; this resolves the prior documentation finding.

The `@AssertTrue` accessor preserves `@NotNull` as the null validator and rejects both non-null equality and reverse ordering. The intentionally untracked baseline prevents a Git-range scope comparison, so scope was assessed from the supplied file list and current sources.

<details>
<summary>Issues (0)</summary>

No actionable Critical, Important, or Minor findings.

</details>

<details>
<summary>Details</summary>

## Pricing snapshot ownership and JSON boundary

`src/main/java/id/xyz/parkease/dto/PricingSnapshot.java:5-13` declares the specified annotation-free record with all seven components in the required order. The repository-wide reference trace found no `id.xyz.parkease.domain.PricingSnapshot` import; the mapper and both tests instead import the DTO (`PricingSnapshotMapper.java:5`, `PricingSnapshotMapperTest.java:5`, and `PricingSnapshotTest.java:3`). `PricingSnapshotMapper.java:9-35` is a record, rejects a null component in its compact constructor, and retains the existing `IllegalArgumentException` messages for blank JSON and Jackson serialization/deserialization failures.

## Slot-level temporal integrity

`V1__initial_schema.sql:45-46` defines the planned endpoints as `TIMESTAMP WITH TIME ZONE`; `V2__add_exclusion_constraint.sql:1-7` consequently uses `tstzrange(planned_start, planned_end, '[)')`, `slot_id WITH =`, and `WHERE (status IN ('PENDING', 'ACTIVE'))`. The V1 documentation at lines 81-83 now accurately names the V2 constraint and its scope, replacing the phantom-table documentation identified in the prior Task 2 review. The V2 constraint comment at lines 9-10 states the terminal-history and touching-endpoint behavior. `MigrationContractTest.java:25-44` checks the static range, slot, bounds, and status terms; `ReservationExclusionIT.java:76-114` covers overlap rejection, endpoint touching, terminal history, and different slots when Docker is available.

## Planned-window validation boundary

`Reservation.java:53-59` retains `@NotNull` on both planned endpoints, and lines 100-102 add the null-safe `@AssertTrue` predicate. With both fields populated, `isBefore` makes equality and reverse ordering invalid. `DomainValidationTest.java:42-50` uses fixed timestamps to prove the equality boundary; reverse ordering follows from the same strict predicate. No service, controller, repository, lifecycle transition, pricing, Redis, promotion, audit, demand-pricing, or extra Flyway migration appears among the supplied remediation files.

## Verification evidence and limits

The final package records `./gradlew clean test jacocoTestReport --no-daemon` as 46 passing tests with four Docker-gated PostgreSQL integration tests skipped because Docker was absent, plus a clean `git diff --check`. Those commands were intentionally not repeated. `git status --short` confirms the documented uncommitted-baseline scenario: source, migration, and test directories are untracked, so a Git diff cannot establish a complete change set or attribute unrelated untracked files to this remediation.

</details>

<details>
<summary>File map</summary>

- `src/main/java/id/xyz/parkease/dto/PricingSnapshot.java` — seven-field immutable pricing DTO.
- `src/main/java/id/xyz/parkease/mapper/PricingSnapshotMapper.java` — null-safe record-based Jackson boundary.
- `src/test/java/id/xyz/parkease/mapper/PricingSnapshotMapperTest.java` and `src/test/java/id/xyz/parkease/domain/PricingSnapshotTest.java` — DTO and mapper regression coverage.
- `src/main/resources/db/migration/V1__initial_schema.sql` and `V2__add_exclusion_constraint.sql` — accurate V2 documentation and zoned exclusion constraint.
- `src/test/java/id/xyz/parkease/db/MigrationContractTest.java` and `ReservationExclusionIT.java` — static and Docker-gated database proof.
- `src/main/java/id/xyz/parkease/domain/Reservation.java` and `src/test/java/id/xyz/parkease/domain/DomainValidationTest.java` — cross-field planned-window invariant and equality regression test.

Full review basis: `.superpowers/sdd/2026-09-06-parkease-sprint-1-remediation/final-review-package.md`.

</details>
