# Zoned PostgreSQL reservation range correction

The change replaces V2’s incompatible `tsrange` constructor with `tstzrange` for `TIMESTAMP WITH TIME ZONE` reservation columns and adds a migration contract assertion for that range type. The executable constraint retains `slot_id WITH =`, half-open `[)` bounds, and the `PENDING`/`ACTIVE` partial predicate; no additional migration is present in the supplied review scope. The revised `COMMENT ON TABLE reservation` accurately states those executable properties. **Watch for:** **confirmed** V1 still labels the documentation block as a `parking_slot_exclusion_constraint` table and claims that table stores the constraint definition, but no such table exists and V2 actually creates the constraint with `ALTER TABLE`. This leaves V1 documentation inconsistent with executable V2 behavior.  
**Verdict**: NEEDS_CHANGES

## High-level view

V2 now constructs the exclusion range with PostgreSQL’s timezone-aware `tstzrange`, matching V1’s `TIMESTAMP WITH TIME ZONE` reservation columns. Its scope, overlap semantics, and active-reservation predicate remain unchanged, so the correction is limited to the required type fix.

The V1 reservation comment now names the V2 constraint behavior correctly, but it remains underneath a phantom table heading whose explanatory text contradicts the actual migration topology. The documentation correction must include that surrounding block, not only the `COMMENT ON TABLE` string.

The supplied report documents a red-to-green contract test and a Docker-unavailable Testcontainers skip. Those commands were not rerun, as required; the Docker skip is permitted by the task brief and the report gives the required `/var/run/docker.sock` evidence.

<details>
<summary>Issues (1)</summary>

1. **MEDIUM / MISIMPLEMENTATION — Phantom constraint-table documentation** — **confirmed** V1 lines 79–83 describe `parking_slot_exclusion_constraint` as a table holding the constraint definition, while V2 lines 1–7 adds `no_overlapping_reservations` directly to `reservation`. Replace the heading and explanatory comments with documentation of the V2 `ALTER TABLE` constraint (or remove the obsolete block) while retaining the accurate reservation-table comment.

</details>

<details>
<summary>Details</summary>

## Phantom V1 constraint-table documentation

**MEDIUM / MISIMPLEMENTATION — confirmed.** `V1__initial_schema.sql:79–83` says `Table: parking_slot_exclusion_constraint`, describes it as a table “used only for the EXCLUDE constraint,” and says the constraint definition is stored there. The same file creates no table with that name. The executable implementation is `V2__add_exclusion_constraint.sql:1–7`, which performs `ALTER TABLE reservation ADD CONSTRAINT no_overlapping_reservations EXCLUDE USING GIST (...)`.

Although V1 line 85 now accurately describes the `slot_id`-scoped, half-open `tstzrange` behavior for `PENDING` and `ACTIVE` reservations, its immediately preceding documentation gives a false schema object and a false account of where the definition resides. This fails the binding requirement that V1 documentation accurately describe executable V2 behavior. A comment-only correction is sufficient and preserves the no-structure-change constraint.

## Zoned constraint contract and Docker-gated runtime coverage

`V1__initial_schema.sql:45–46` declares `planned_start` and `planned_end` as `TIMESTAMP WITH TIME ZONE`. `V2__add_exclusion_constraint.sql:4–7` applies `tstzrange(planned_start, planned_end, '[)') WITH &&`, retains `slot_id WITH =`, and retains `WHERE (status IN ('PENDING', 'ACTIVE'))`. `MigrationContractTest.java:35–37` requires `tstzrange`; lines 25–31 independently retain checks for `[)` and `slot_id WITH =`, and lines 40–44 check the active-status terms. This satisfies the stated static contract requirements.

Not tested in this review: a live PostgreSQL execution of the four integration cases. Per `task-2-report.md`, Testcontainers could not access `/var/run/docker.sock`, all four cases were skipped, and the migration contract suite passed; the task brief explicitly permits that skip only under this Docker-unavailable condition. The reported commands were intentionally not rerun.

</details>

<details>
<summary>File map</summary>

- `src/main/resources/db/migration/V1__initial_schema.sql` — timezone-aware reservation columns and revised, but internally inconsistent, constraint documentation.
- `src/main/resources/db/migration/V2__add_exclusion_constraint.sql` — executable slot-scoped `tstzrange` exclusion constraint.
- `src/test/java/id/xyz/parkease/db/MigrationContractTest.java` — static assertion that V2 contains `tstzrange`.
- `src/test/java/id/xyz/parkease/db/ReservationExclusionIT.java` — Docker-gated PostgreSQL behavioral coverage, unchanged in the supplied scope.

Full change evidence is supplied in `task-2-review-package.md`; repository range diff was unavailable because task files are untracked.

</details>
